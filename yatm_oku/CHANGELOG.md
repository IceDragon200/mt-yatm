# 0.7.0

* Refactored Computers service
  * Detached computers are now support, that is, instances that aren't fixed to a particular position in the world, these can be used by entities that need to run emulation without the usual node assumptions
